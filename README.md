# Copperview Calling Change

Static GitHub Pages site for submitting calling change requests for the Copperview Stake.

Live: https://bkjems.github.io/copperview-calling-change/

The backend is a Google Apps Script Web App (not stored in this repo). The deployed Web App URL is set in `calling-change.js` as `APPS_SCRIPT_URL`.
